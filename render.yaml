services:
  - type: web
    name: ai-rank-bot
    env: python
    plan: free
    pythonVersion: 3.11.9        # <— ВАЖНО
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
